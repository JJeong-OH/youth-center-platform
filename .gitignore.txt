# 환경 변수 (절대 올리면 안됨!)
   .env
   .env.local
   .env*.local
   **/.env
   **/.env.local

   # 의존성
   node_modules/
   **/node_modules/

   # 빌드 결과물
   dist/
   build/
   .next/
   out/

   # 로그
   *.log
   npm-debug.log*
   yarn-debug.log*
   yarn-error.log*

   # OS 파일
   .DS_Store
   Thumbs.db
   desktop.ini

   # IDE
   .vscode/
   .idea/
   *.swp
   *.swo

   # 데이터베이스
   *.db
   *.sqlite
   *.sqlite3

   # 테스트
   coverage/
   .nyc_output/

   # 프리즈마
   prisma/migrations/

   # Docker
   docker-compose.override.yml